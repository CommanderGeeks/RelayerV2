export interface INotifyData {
  // status: string;
  deposited: string;
  received: string;
  fromChainName: string;
  toChainName: string;
  // message: string;
  inputTx: string;
  outputTx: string;
}
